<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>com.boots.cms.beautifulyou</groupId>
    <artifactId>es</artifactId>
    <version>1.0.11</version>
    <relativePath>../pom.xml</relativePath>
  </parent>

  <artifactId>es-test-data</artifactId>
  <packaging>pom</packaging>
  <profiles>
    <profile>
      <id>development-themes</id>
      <activation>
        <property>
          <name>!skipThemes</name>
        </property>
      </activation>
      <dependencies>
        <dependency>
          <groupId>${project.groupId}</groupId>
          <artifactId>basic-theme</artifactId>
          <version>${project.version}</version>
          <classifier>theme</classifier>
          <type>zip</type>
        </dependency>
        <dependency>
          <groupId>${project.groupId}</groupId>
          <artifactId>es-theme</artifactId>
          <version>${project.version}</version>
          <classifier>theme</classifier>
          <type>zip</type>
        </dependency>
      </dependencies>

      <build>
        <plugins>
          <!-- Create Webresource content in target folder. -->
          <plugin>
            <groupId>com.coremedia.maven</groupId>
            <artifactId>coremedia-webresource-content-maven-plugin</artifactId>
            <!-- Provide doctypes to create web resources as content-->
            <dependencies>
              <dependency>
                <groupId>com.boots.cms</groupId>
                <artifactId>contentserver-blueprint-component</artifactId>
                <version>${boots.cms.core.version}</version>
              </dependency>
            </dependencies>
            <executions>
              <execution>
                <id>compile-resource-es-testdata</id>
                <goals>
                  <goal>compile</goal>
                </goals>
                <phase>compile</phase>
              </execution>
            </executions>
          </plugin>
          <!-- Build testdata zip-->
          <plugin>
            <artifactId>maven-assembly-plugin</artifactId>
            <executions>
              <execution>
                <id>create-es-test-data-zip</id>
                <goals>
                  <goal>single</goal>
                </goals>
                <phase>package</phase>
                <configuration>
                  <appendAssemblyId>false</appendAssemblyId>
                  <finalName>test-data</finalName>
                  <descriptors>
                    <descriptor>src/main/assembly/test-data-assembly-descriptor.xml</descriptor>
                  </descriptors>
                </configuration>
              </execution>
            </executions>
          </plugin>
        </plugins>
      </build>
    </profile>
  </profiles>
  <build>
    <plugins>
      <!-- We have to skip the enforcer because we have a bug in the OneRepoRule which checks dependencies -->
      <plugin>
        <artifactId>maven-enforcer-plugin</artifactId>
        <groupId>org.apache.maven.plugins</groupId>
        <configuration>
          <skip>true</skip>
        </configuration>
      </plugin>
    </plugins>
  </build>
</project>