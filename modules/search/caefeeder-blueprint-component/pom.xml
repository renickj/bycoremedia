<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>com.boots.cms.beautifulyou</groupId>
    <artifactId>search</artifactId>
    <version>1.0.19</version>
  </parent>

  <artifactId>caefeeder-blueprint-component</artifactId>

  <dependencies>
    <dependency>
      <groupId>com.boots.cms</groupId>
      <artifactId>contentbeans</artifactId>
      <version>${boots.cms.core.version}</version>
      <scope>runtime</scope>
      <exclusions>
        <!-- Exclude ASM 3.3.1, which is incompatible with Tika 1.7 -->
        <exclusion>
          <groupId>asm</groupId>
          <artifactId>asm</artifactId>
        </exclusion>
      </exclusions>
    </dependency>

    <dependency>
      <groupId>${project.groupId}</groupId>
      <artifactId>caefeeder-lib</artifactId>
      <version>${project.version}</version>
      <scope>runtime</scope>
    </dependency>

    <dependency>
      <groupId>${project.groupId}</groupId>
      <artifactId>cae-base-lib</artifactId>
      <version>${project.version}</version>
      <scope>runtime</scope>
      <exclusions>
        <!-- Exclude ASM 3.3.1, which is incompatible with Tika 1.7 -->
        <exclusion>
          <groupId>asm</groupId>
          <artifactId>asm</artifactId>
        </exclusion>
      </exclusions>
    </dependency>

    <dependency>
      <groupId>${project.groupId}</groupId>
      <artifactId>caefeeder-extension-dependencies</artifactId>
      <version>${project.version}</version>
      <type>pom</type>
      <scope>runtime</scope>
      <exclusions>
        <!-- Exclude ASM 3.3.1, which is incompatible with Tika 1.7 -->
        <exclusion>
          <groupId>asm</groupId>
          <artifactId>asm</artifactId>
        </exclusion>
      </exclusions>
    </dependency>

    <dependency>
      <groupId>com.boots.cms</groupId>
      <artifactId>caefeeder-extension-dependencies</artifactId>
      <version>${boots.cms.core.version}</version>
      <type>pom</type>
      <scope>runtime</scope>
      <exclusions>
        <!-- Exclude ASM 3.3.1, which is incompatible with Tika 1.7 -->
        <exclusion>
          <groupId>asm</groupId>
          <artifactId>asm</artifactId>
        </exclusion>
      </exclusions>
    </dependency>

    <dependency>
      <groupId>com.coremedia.cms</groupId>
      <artifactId>cap-client-component</artifactId>
      <scope>runtime</scope>
    </dependency>

    <dependency>
      <groupId>com.coremedia.cms</groupId>
      <artifactId>cae-linkservices-impl</artifactId>
      <scope>runtime</scope>
    </dependency>

    <dependency>
      <groupId>com.coremedia.cms</groupId>
      <artifactId>caefeeder-solr-component</artifactId>
      <scope>runtime</scope>
    </dependency>

    <dependency>
      <groupId>com.coremedia.blueprint.base</groupId>
      <artifactId>bpbase-multisite-impl</artifactId>
      <scope>runtime</scope>
    </dependency>
    <dependency>
      <groupId>com.coremedia.blueprint.base</groupId>
      <artifactId>bpbase-uapi-client</artifactId>
      <scope>runtime</scope>
    </dependency>

    <dependency>
      <groupId>com.boots.cms</groupId>
      <artifactId>database-drivers</artifactId>
      <version>${boots.cms.core.version}</version>
      <type>pom</type>
      <scope>runtime</scope>
    </dependency>
  </dependencies>
</project>
