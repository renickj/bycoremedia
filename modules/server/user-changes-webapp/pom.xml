<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>
  <parent>
    <groupId>com.boots.cms</groupId>
    <artifactId>server</artifactId>
    <version>1.0-SNAPSHOT</version>
  </parent>
  <artifactId>user-changes-webapp</artifactId>
  <packaging>war</packaging>
  <dependencies>
    <dependency>
      <groupId>${project.groupId}</groupId>
      <artifactId>user-changes-extension-dependencies</artifactId>
      <version>${project.version}</version>
      <type>pom</type>
      <scope>runtime</scope>
    </dependency>
    <dependency>
      <groupId>com.coremedia.ui.collaboration</groupId>
      <artifactId>user-changes-component</artifactId>
      <scope>runtime</scope>
    </dependency>
    <dependency>
      <groupId>com.coremedia.cms</groupId>
      <artifactId>management-component</artifactId>
      <scope>runtime</scope>
    </dependency>

    <dependency>
      <groupId>com.coremedia.blueprint.base</groupId>
      <artifactId>bpbase-multisite-impl</artifactId>
      <scope>runtime</scope>
    </dependency>
  </dependencies>
  <build>
    <finalName>user-changes</finalName>
    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-war-plugin</artifactId>
        <configuration>
          <failOnMissingWebXml>false</failOnMissingWebXml>
        </configuration>
      </plugin>
      <plugin>
        <groupId>org.apache.tomcat.maven</groupId>
        <artifactId>tomcat7-maven-plugin</artifactId>
        <configuration>
          <port>41085</port>
          <warSourceDirectory>src/main/webapp</warSourceDirectory>
          <path>/</path>
          <systemProperties>
            <catalina.base>${project.build.directory}</catalina.base>
            <resourcePath.base>${project.basedir}</resourcePath.base>
            <coremedia.application.repository.url>
              http://${cms.host}:${CMS_PORT_PREFIX}${HTTP_PORT_SUFFIX}/coremedia/ior
            </coremedia.application.repository.url>
            <coremedia.application.repository.user>webserver</coremedia.application.repository.user>
            <coremedia.application.repository.password>webserver</coremedia.application.repository.password>
            <!--
               Sets a JMX remote url so that the webapp can be accessed via JMX when running with tomcat plugin.
               When running this application in a standalone tomcat, then the tomcat's built-in remote connector server
               can be used instead.
            -->
            <coremedia.application.management.server.remote.url>service:jmx:rmi://localhost:41089/jndi/rmi://localhost:${STUDIO_PORT_PREFIX}${JMX_REGISTRY_PORT_SUFFIX}/user-changes</coremedia.application.management.server.remote.url>
            <coremedia.logging.directory>${project.build.directory}/logs</coremedia.logging.directory>
            <mongoDb.clientURI>mongodb://${mongo.db.host}:${mongo.db.port}</mongoDb.clientURI>
          </systemProperties>
        </configuration>
      </plugin>
    </plugins>
  </build>
</project>
